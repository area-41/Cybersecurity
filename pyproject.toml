[project]
name = "codigos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Área 41", email = "email.diferente.victor@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyaes>=1.6.1",
]

[project.scripts]
codigos = "codigos:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
